let header = document.querySelector("h1") // no . or # will search by tag name

let list = document.querySelector(".swbats") // . denotes search by class

let image = document.querySelector("#dom") // # denotes search by id

let listElements = document.querySelectorAll("li")

// we cannot call all array methods on collection(nodeList) returned by querySelectorAll